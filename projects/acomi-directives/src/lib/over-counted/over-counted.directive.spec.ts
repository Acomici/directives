import { OverCountedDirective } from './over-counted.directive';

describe('OverCountedDirective', () => {
  it('should create an instance', () => {
    const directive = new OverCountedDirective();
    expect(directive).toBeTruthy();
  });
});
